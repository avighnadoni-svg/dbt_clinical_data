version: 2

models:
  - name: dim_patient
    columns:
      - name: patient_id
        tests:
          - not_null
          - unique

  - name: dim_provider
    columns:
      - name: provider_id
        tests:
          - not_null
          - unique

  - name: dim_organization
    columns:
      - name: organization_id
        tests:
          - not_null
          - unique

  - name: dim_payer
    columns:
      - name: payer_id
        tests:
          - not_null
          - unique

  - name: fct_encounter
    columns:
      - name: encounter_id
        tests:
          - not_null
          - unique
      - name: patient_id
        tests:
          - not_null
          - relationships:
              to: ref('dim_patient')
              field: patient_id

  - name: fct_condition
    columns:
      - name: patient_id
        tests:
          - not_null
          - relationships:
              to: ref('dim_patient')
              field: patient_id

  - name: fct_observation
    columns:
      - name: patient_id
        tests:
          - not_null
          - relationships:
              to: ref('dim_patient')
              field: patient_id

  - name: fct_medication
    columns:
      - name: patient_id
        tests:
          - not_null
          - relationships:
              to: ref('dim_patient')
              field: patient_id

  - name: fct_procedure
    columns:
      - name: patient_id
        tests:
          - not_null
          - relationships:
              to: ref('dim_patient')
              field: patient_id

  - name: fct_careplan
    columns:
      - name: careplan_id
        tests:
          - not_null
      - name: patient_id
        tests:
          - not_null
          - relationships:
              to: ref('dim_patient')
              field: patient_id

  - name: fct_device
    columns:
      - name: patient_id
        tests:
          - not_null
          - relationships:
              to: ref('dim_patient')
              field: patient_id

  - name: fct_immunization
    columns:
      - name: patient_id
        tests:
          - not_null
          - relationships:
              to: ref('dim_patient')
              field: patient_id

  - name: fct_imaging_study
    columns:
      - name: imaging_study_id
        tests:
          - not_null
      - name: patient_id
        tests:
          - not_null
          - relationships:
              to: ref('dim_patient')
              field: patient_id

  - name: fct_supply
    columns:
      - name: patient_id
        tests:
          - not_null
          - relationships:
              to: ref('dim_patient')
              field: patient_id

  - name: fct_allergy
    columns:
      - name: patient_id
        tests:
          - not_null
          - relationships:
              to: ref('dim_patient')
              field: patient_id

  - name: fct_payer_transition
    columns:
      - name: patient_id
        tests:
          - not_null
          - relationships:
              to: ref('dim_patient')
              field: patient_id
      - name: payer_id
        tests:
          - not_null
          - relationships:
              to: ref('dim_payer')
              field: payer_id